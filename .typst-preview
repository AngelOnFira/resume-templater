# Include only the main resume file
include = ["modernpro-cv/resume.typ"]

# Exclude all other .typ files
exclude = [
    "modernpro-cv/content/*.typ",
    "modernpro-cv/template.typ",
    "modernpro-cv/modernpro-cv.typ"
] 